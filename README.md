# CSV-Grouping-Formatter-Web-App
A lightweight personal web application that allows users to upload CSV files, automatically group rows by unique values in the first column, visually review the formatted output, and export the updated structure as a downloadable CSV.
